import React from 'react';

const recentPosts = (props) => {
    return (
        <div>
            <ul>
                <li>{props.title1}</li>
            </ul>
        </div>
    );
}

export default recentPosts;